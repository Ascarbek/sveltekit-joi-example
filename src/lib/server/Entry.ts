export const SaveEntry = async (username: string, email: string, password: string, confirmPassword: string) => {
  console.log('saving entry...');
  console.log(username, email, password, confirmPassword);
};
